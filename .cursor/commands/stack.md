# /stack
Always read docs/ai/SCOPE_OF_WORK.md first and follow it strictly.
If SCOPE_OF_WORK.md is missing, create it with placeholders and STOP.

Then run the full workflow:
ROUTER -> LOAD CONTEXT -> PLAN -> BUILD -> QA -> FIX LOOP (once)

Never implement or suggest out-of-scope items (see SCOPE GUARD).
